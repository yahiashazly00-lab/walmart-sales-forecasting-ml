# ══════════════════════════════════════════════════════════════════════════════
#  XGBOOST NOTEBOOK CELLS — Append after Section 17.5
#
#  These cells form Sections 18–20 of the notebook.
#  All existing pipelines (RF norm, RF raw, Linear Regression) are UNCHANGED.
#  Paste each fenced block as a new notebook cell in order.
#
#  Prerequisites (already defined in earlier sections):
#    X_train_base, X_test_base, y_train, y_test
#    X_train_norm, X_test_norm           (Section 12 — reused for comparison)
#    ml_scaler, NORM_COLS                (Section 12)
#    rf_norm, rf_raw, lr_model           (Sections 12–17)
#    mae_norm, rmse_norm, r2_norm, r2_norm_tr
#    mae_raw,  rmse_raw,  r2_raw,  r2_raw_tr
#    mae_lr,   rmse_lr,   r2_lr,   r2_lr_tr
#    save_dir, sns, plt, pd, np, joblib, json, os
# ══════════════════════════════════════════════════════════════════════════════
